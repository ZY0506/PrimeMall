Name: admin-api
Host: 0.0.0.0
Port: 8088
Mode: dev

JwtAuth:
  AccessSecret: ${JWT_SECRET}
  AccessExpire: 7200
  RefreshSecret: ${JWT_SECRET}

AdminRpc:
  Etcd:
    Hosts:
      - ${ETCD_HOST}:${ETCD_PORT}
    Key: admin.rpc
  Timeout: 5000
